# Resource Manager

The resource manager enforces the shared-resource ordering produced by the SMT
scheduler. Robots that are not next in the proven resource order wait outside
the resource boundary.

